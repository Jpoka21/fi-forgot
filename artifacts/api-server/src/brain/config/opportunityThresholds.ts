/**
 * Brain-owned opportunity reasoning thresholds.
 */

/** Days since last card timeline activity before recommending a card gap opportunity. */
export const CARD_GAP_THRESHOLD_DAYS = 120;

/** Days since a recent accomplishment fresh update before follow-up opportunity fades. */
export const ACCOMPLISHMENT_FOLLOW_UP_THRESHOLD_DAYS = 30;
