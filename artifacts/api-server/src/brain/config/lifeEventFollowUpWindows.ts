/**
 * Follow-up window days by life event type.
 *
 * Does not map question keys — see lifeEventQuestionKeyMapping.ts.
 */

export const LIFE_EVENT_FOLLOW_UP_WINDOWS: Record<string, number> = {
  family_update: 30,
};
