# Preparation verification review

This mandatory root review records preparation evidence, not PostgreSQL, authenticated, browser, provider, or owner-workflow evidence. Independent acceptance must reject treating preparation evidence as those real outcomes.

## Finding disposition

| Findings | Resolution and executable evidence |
|---|---|
| 1, 15, 16, 20 | `plan.ts`, `runner.ts`, `execution.ts`, and `future-workflow.ts` implement a plan-first typed-argv lifecycle: initialize/start, separate cluster-admin and least-privilege app roles, create, identity, baseline, exact fixture load, API capture/stop/restart, assertions, and stop/recovery. Unknown arguments fail. Process-only credentials never enter argv. `test-execution.ts` proves zero calls in plan/denied modes, observed target/version/hash failures, order, identity comparison, stop-on-error, and recovery with a fake adapter. |
| 2, 11, 22 | The admitted scripts separately run static/schema parity, containment/executor and original production integration tests, schema-shaped fixture checks, the consolidated gate, and original production regressions. `run-regressions.ts` preserves the original tests and corrects only cwd/tsx/tsconfig launch isolation. Synthetic and controlled-adapter results remain explicitly non-real. |
| 3, 19 | Drizzle and loose SQL use the supported named `UNIQUE NULLS NOT DISTINCT` constraint; named follow-through checks and the temporal recipient index align. `verify-schema-parity.ts` compares the complete exported snapshot and regenerated SQL in memory. |
| 4, 9 | Repository-local offline serialization produced the reviewed 41-table/64-statement baseline. Fresh creation applies it only. The manifest records its hash and blocks incremental upgrade without later predecessor/catalog proof. No database API is called. |
| 5, 10, 21 | Fixtures use actual users, recipients, personal-recipients, and question-answer shapes for two owners and seven distinct recipients, including four distinct date-bearing candidates and a restrained/null case. The future loader writes those production tables and later derives Opportunities through production routes; it never seeds Opportunity facts. Original production regressions cover memory/versioning, interpretation/hypothesis, temporal, feedback/follow-through, correction/replay/history, ownership, and maximum-three/no-backfill with controlled repositories. The future runner rechecks owner/recipient timeline, feedback, follow-through, projection cap, and restart state when separately authorized. |
| 6, 8, 18 | Pure preflight precedes unsafe imports. Explicit qualification mode installs the actual process fetch, HTTP(S), socket, and subprocess guards; binding/origins are loopback/fixed-port only. Tests inject the transports actually installed and deny bypasses without connecting. Production mode is unchanged. Documentation states JavaScript guards are not an OS/browser firewall. |
| 7, 17 | Execution-admitted composition allowlists real personal-recipient/session, v2 recipient Brain lifecycle, and v2 Concierge routers; providers, jobs, seeds, webhooks, and delivery routes are absent. Complete target/hash/catalog/disposable preflight is required. |
| 12 | `scripts/package.json` supplies `brain-qualification:plan` and all five exact admitted verification names. The README uses them. |
| 13 | Initial restoration was byte-compared with the saved source. Published checks use the committed historical SHA-256 and no `.orchestra` dependency. Exact-byte/LF hashing exposes line-ending drift. |
| 14 | Procedure/tests use production `/concierge` and state the preseeded caller-supplied identity limitation without claiming authentication. |

## Deliberately unqualified residuals

- No PostgreSQL binary/server/catalog, migration, fixture load, API workflow, browser, provider, authentication, or external network was used.
- Fresh inspection rejects existing PGDATA. Upgrade remains unavailable until independently observed predecessor/catalog evidence exists.
- The system adapter requires new exact owner authorization plus fixed target, identity, artifact hashes, disposability, catalog, credentials, and external OS-boundary evidence. Tests use fake adapters only.
- Recovery is recreation of the exact proven disposable target because no reviewed down chain exists. Ambiguous ownership or writes stop for receipt inspection.
- Known-ID impersonation remains possible; authenticated isolation is not qualified.
- Concierge CUA evidence remains unresolved pending separately authorized PostgreSQL execution and safe supported desktop browser access.

All original concrete findings 1–22 are resolved at preparation scope by the evidence above. This permits independent inspection; it does not grant acceptance, checkpoint, publication, or future execution authority.

## Root follow-up disposition

The later concrete review was corrected as follows:

- Direct fixture loading now validates the committed fixture hash, exact credential-bearing loopback URL, exact owned marker, and observed database, role, server address/port, PostgreSQL major, and absolute data directory before its first transaction. The pool closes in a `finally` block.
- Initial assertions reject empty production projections, execute idempotent production feedback, action, and outcome mutations, capture histories/timelines, and create an exclusive durable snapshot. `--after-restart` replays the same receipts and requires exact durable-state equality. The original production regression group remains the preparation-only evidence for edit/archive/restore, interpretation/hypothesis, correction, conflict, immutable history, cap, and no-backfill logic.
- The plan launches the built API through the known Node entry, probes its qualification health before assertions, captures the exact child, waits for termination before restart, and repeats readiness/assertion phases. Cluster ownership is marked after initialization.
- Installed-process tests directly invoke CommonJS and pre-imported ESM HTTP, HTTPS, HTTP2, TLS, Socket/connect/createConnection, UDP, DNS callback/promise/resolver, subprocess, worker, native-addon, fetch, and WebSocket entry points. Production mode is verified as a no-op. The planner no longer manufactures OS-boundary approval; execution requires it as separately supplied evidence. This remains process containment, not an OS firewall claim.
- Recovery stops the captured API and PostgreSQL, then recursively removes only the exact PGDATA whose exact ownership marker was created in the completed lifecycle. A subsequent full execution recreates from the reviewed baseline; unknown or unmarked targets are never removed. This code is covered with fake adapters and is not executed in preparation.

These corrections close the follow-up implementation findings at preparation scope. Independent acceptance still must rerun the admitted evidence and must not report real PostgreSQL persistence, authentication, browser qualification, or provider behavior.

## Continuation root containment implementation

Root now owns/has implemented runtime-containment.ts, understanding-router.ts, test-installed-containment.ts and test-provider-graph.ts. Installed Node transport denial test passed 26 entry points without any external connection and verified production no-op/idempotence. Offline esbuild graph inspection passed 134 source modules without execution: actual production versioned-understanding and Concierge remain, broad v2-recipients import was removed because it eagerly imported lib/openai through follow-up-questions. The qualification router now exposes the same production lifecycle handlers via understanding-router.ts, retaining paths and x-user-id semantics. Keep this graph and integrate the target gate from the worker. Root moved index/app logger initialization after hooks; qualification uses production synchronous Pino sink to prevent worker startup. Two focused commands are appended to the admitted test script. These are real installed-boundary and offline graph checks, NOT PostgreSQL or browser proof. Root continues frontend containment inspection. Preserve original schema/baseline, tests, and all authority boundaries.
Root containment follow-up: installed-boundary test now passes 27 real Node/ESM entry-point denials; seed guard imports zero providers in qualification modes and preserves normal injected delegation. Qualification frontend receives explicit CSP limiting connections to self/API8080/HMR25460, blocking workers/frames/external images/fonts; Replit runtime overlay/banner/cartographer are disabled in qualification mode only. No browser was launched. browser-policy.ts and vite.config.ts changes are within existing scope. Root fixed a local runtime-containment type annotation after typecheck feedback; retain these focused tests and rerun both typechecks. Tests do not claim OS-wide containment or actual browser qualification. Do not generate a fake reviewed OS boundary flag; API graph has only controlled Node runtime capabilities and explicit limitations.

Root target guard contribution: target-verification.ts + test-target-verification.ts now PASS offline. Strict URL gate rejects query/fragment options (a potential lib/pg connection override), wrong host/port/database/role; observed identity requires server address/port, exact PostgreSQL16 version range and actual data directory, and zero action calls on unknown/missing identity, artifact hash or owned-directory failure. Integrate this shared guard into every direct fixture load and destructive path, do not leave it as an unused test helper. An application-role connection may not have permission to read data_directory: inspect with the separately approved local cluster-admin credentials and compare actual server address/port/start identity with the app connection, rather than granting production privileges or echoing the expected path. No DB may be connected now. Retain root containment tests and router graph; all API/frontend typechecks now pass.

Root continuation blocking findings (must not mark resolved by existing mocked regression): future-workflow.ts still lacks edit/archive/restore, interpretation and hypothesis lifecycle mutations. Existing fixture triggerType event_briefing is immutable (handlers accept profile_gap/fresh_update/follow_up); seed a truthful synthetic communication observation suitable for these handlers and update fixture hashes. Do not replay mutations after restart: after-restart must only re-read histories and compare stable persisted state. Fixed 2031 birthdays versus actual API clock need explicit fixture-relative run dates or documented supported clock seam without fabricating observation timestamps. Direct loader must reject URL query/hash overrides using target-verification.ts and actually compute independent identity/disposability proof. Current app role cannot necessarily read data_directory: fail-closed is safe but not executable happy path; use separate approved admin inspection and matching server identity. execution.ts recover still recursively deletes based only on static marker and completed phase, not independently observed exact target, and no graceful/forced awaited cleanup. Remove any claims these are resolved until implemented and tested. These remain the same four authorized correction areas.
Root added lifecycle-assertions.ts: executable future HTTP edit/archive/restore, interpretation actions/supersession, hypothesis retirement/new-content revival/user control, history and cross-owner mutation denial. It requires qa-a-active profile_gap communication source, remains unintegrated until future-workflow invokes qualifyUnderstanding BEFORE initial snapshot only. Its assertions must be tested offline and actual PG execution remains deferred. Worker must integrate it, fix fixture/hash, and keep after-restart read-only. Do not claim it executed real persistence.
Root lifecycle harness now PASSES using existing UnderstandingPgFixture and actual production handlers (no DB), caught and corrected unsupported revival wording. Corrected qa-a-active to profile_gap/communication; updated hash in README and loader. Admitted test script includes this harness. Shared strict URL guard now called in direct future-workflow before DB imports. Remaining load observed-admin identity/recreation and future runner wiring still require correction. Controller verification may have begun before these edits, so complete retest is required.

## Current root handoff: complete package NOT accepted
The root edited candidate files while the independent acceptance review was running, and the controller correctly stopped with "Acceptance review modified the managed project". This is an overlapping root edit, not a requested controller change. Root is now stopping edits so the worker/controller can perform a stable correction and complete fresh verification/acceptance.
New root changes since the worker: lifecycle-assertions.ts and offline test use actual production handlers with the existing substitute. Corrected editable communication fixture/hash. future-workflow initial phase invokes lifecycle and feedback/completion/outcome mutations; after-restart reads only and compares histories (not replay mutations). Strict URL gate is called. no-db-environment allowlists OS paths only, preventing inherited credentials/DB/preloads in the complete gate; safer full gate passed. Awaited process-lifecycle helper has passing fake graceful/forced/port/readiness checks. Execution driver is now async; runner awaits it. API supports qualification-only IPC graceful listener/pool shutdown. Focused tests included in admitted script.
## Worker disposition of the final four blockers

- The direct loader now requires independently strict admin and application URLs. A short-lived cluster-admin pool observes the maintenance database, admin role, server address/port, PostgreSQL 16 range, and exact data directory, then closes. Only afterward does a separately opened least-privilege app pool verify its database, role, address, port, and version before the first transaction. The fixture hash and exact owned marker are verified before either import.
- Failure recovery uses the same awaited graceful/forced API shutdown and port-release helper as the planned stop. PostgreSQL uses bounded `pg_ctl --wait` shutdown. Recursive removal occurs only after rereading the exact real PGDATA, exact marker and `PG_VERSION=16`, and independently proving port 55432 is released. Failure of any observation refuses removal. The ordinary exact executor is the recreate flow after verified removal; it rechecks target absence and rebuilds only from the reviewed baseline.
- The future initial assertion invokes the complete production-handler understanding lifecycle, then verifies feedback, completed action, separate outcome receipts/active states, negative cross-owner mutations, raw timeline/history projections, and owner-scoped active/history reads. The post-restart branch performs no mutations; it rereads and deep-compares the recorded histories, active states, receipts, opportunity identities, owners, and resolved run date.
- The fixture now records qualification-only birthday offsets separately from unchanged production row shapes. Execution requires an explicit valid synthetic run date, resolves the candidate birthdays relative to it, records that date in durable evidence, and leaves source observation times null. The readiness probe now uses the shared bounded helper and a per-request timeout.

These were preparation implementation corrections only. The admitted no-database tests use controlled substitutes/fake processes and do not claim that the future PostgreSQL lifecycle, recreation, persistence, browser workflow, or authentication has occurred. Fresh independent acceptance remains required on a stable worktree.

Root review of correction attempt 3: tsx-safe-launcher.ts currently defines process.geteuid to return 0. Do not claim this is a harmless environment fix: it spoofs effective root identity for every dependency and can change unrelated privilege-sensitive behavior. Remove this identity-spoofing approach. Use repository-local installed compilation/runtime tooling with a narrowly scoped cache-path solution or native TypeScript execution; do not download substitute test tooling, alter identities, weaken permissions, or remove the original tests. This note is communication during correction, before verification; root has made no concurrent implementation edits.
Root follow-up: the same blocker also applies to tsx-worker-preflight.ts and tsx-worker-preflight.mjs; the latter now overrides os.userInfo with invented username, home, uid, and gid. Do not resolve this by merely renaming the preloader or labeling it test-only. Remove all broad identity and privilege API overrides before claiming reproducible acceptance. Attempted direct worker message was rejected because the task already has an active writer; this mandatory review note is the durable communication.

## Root recovery after correction limit: native runtime and complete plan parity
The fabricated getuid/userInfo preloader and its runner have been deleted. New native-typescript-loader.mjs/typescript-runner.ts use installed TypeScript and native Node module hooks; exact original tests remain unchanged. The sole nested legacy serialized-consumer command is adapted only after exact path and argument matching; no global identity, network, filesystem or process API is mocked. test-native-runtime.ts compares baseline and loader OS identity behavior including errors without emitting user values. The native scratch proof passed schema parity plus all original integration/regression files. The complete admitted no-database gate then passed under the managed sandbox with no escalation, database, provider, browser or application execution.
The plan now exports its exact execution requirements used by its predicate and its limitations. verify-plan-documentation.ts runs the actual nonmutating plan CLI and deep-compares the entire result with executable-plan.json and the full generated README JSON example. It verifies every target/hash/phase/argv/env/requirement/limitation, not fragments, and never regenerates expected artifacts during a check. Initial assertion phase now truthfully declares mutations; post-restart assertions remain read-only. This addresses both final independent findings, with new concrete executable recovery evidence. Fresh complete controller verification and independent acceptance are required. Root will stop edits before resuming the controller.
