# Brain qualification preparation: ready for independent acceptance

Status updated 2026-09-11. This is a no-database preparation result, not proof of PostgreSQL persistence, authenticated isolation, browser behavior, provider delivery, or the owner workflow. Independent acceptance, checkpointing, and publication remain the outer Orchestra controller's responsibility.

1. **Historical readiness.** `docs/brain-integration-readiness.md` is restored byte-for-byte with committed SHA-256 `103f9166e04f4463c999a713452a59e899d5c43c2fbf85b1c225ede21159af6b`. It remains historical and does not state that loose SQL was applied.

2. **Migration review.** The reviewed manifest orders exactly the five loose SQL artifacts, records hashes, dependencies, classification, transaction/idempotency/rollback status, Drizzle parity, and residual risks. Nullable hypothesis-evidence uniqueness, follow-through CHECK constraints, and the temporal recipient index are aligned offline. No SQL was applied.

3. **Fresh baseline.** The reviewed full-current offline baseline contains 41 tables and 64 statements, SHA-256 `193867eb1a69acb86757ffd2f2340ec02785610be717163170edc855eb9fc7b2`. It is the fresh bootstrap path; the five loose files are not replayed after it. Upgrade remains unavailable until a future authorized predecessor/catalog inspection proves applicability.

4. **Containment.** Explicit qualification mode installs fail-closed runtime guards across fetch, HTTP/HTTPS, raw sockets, TLS, DNS, datagram, HTTP/2, child-process, worker, and cluster entry points before the qualification application graph loads. Fixed loopback hosts/origins and ports are enforced; providers, schedulers, seeds, webhooks, delivery, and unrelated production routes are excluded. Production defaults are unchanged.

5. **Containment limits.** Process-level hooks are defense in depth, not an OS firewall. A later execution requires independently observed loopback-only OS containment; the planner does not self-attest that evidence. Native code, a compromised runtime, browser internals, and processes outside the guarded tree remain outside this preparation proof.

6. **Synthetic fixtures.** Deterministic fixtures cover two owners, seven distinct recipients, provenance, nullable evidence, source/version/cycle identity, confidence, observation timing, corrections, immutable history, action reports, outcomes, and external verification. Approved fixture SHA-256 is `0e0df18b78c6e14c126d7f392eb9e101e4f9228f3f89173e646b2f1d0270fac8`.

7. **Production-path assertions.** Controlled no-DB tests exercise actual Brain derivation and repository contracts, owner/recipient filtering, serialization, lifecycle operations, restraint, and at-most-three original eligible presentation slots without backfill. Synthetic timing is resolved only from an explicit qualification run date. These tests do not claim persisted Opportunities.

8. **Identity boundary.** Repository filtering is tested for distinct synthetic owners and recipients. Development identity headers remain caller supplied; known-ID impersonation is possible, so authenticated isolation is deliberately unqualified.

9. **Future target.** The only prepared target is PostgreSQL 16 at `127.0.0.1:55432`, database `fi_forgot_brain_qualification`, application role `fi_forgot_brain_qualifier`, cluster-admin role `fi_forgot_brain_cluster_admin`, PGDATA `C:/Users/James.Massaro/Projects/fi-forgot/.orchestra/qualification/fi-forgot-brain-qualification-pgdata`, API `127.0.0.1:8080`, and frontend `127.0.0.1:25460`. Credentials are neither created nor stored.

10. **Execution admission.** Typed argument-vector plans default to non-mutating output. Mutation requires the exact future owner token and flag plus independently observed server version/address/port/data directory, separate admin and application identities, approved artifact hashes, disposable marker/ownership evidence, strict URLs, explicit synthetic run date, and predecessor evidence when upgrading. Direct loader invocation cannot bypass admission or inherit an arbitrary `DATABASE_URL`.

11. **Future lifecycle.** Guarded adapters implement creation, migration, fixture load, lifecycle mutation/assertion, evidence capture, awaited API readiness/stop/restart against the same target, after-restart read-only durable-state comparison, bounded cleanup, rollback/recreate recovery, and independent recreation verification. Pools and child processes are closed or awaited. None was executed in this mission.

12. **Concierge contract.** The unresolved future procedure targets `http://127.0.0.1:25460/concierge`, names synthetic identity/relationship data and Opportunity states, checks restraint and maximum-three behavior, requires supported desktop CUA accessibility-tree and sanitized screenshot evidence, and compares browser presentation with API/server projections before Orchestra handoff. No browser session was opened.

13. **Documentation drift.** Published examples use the admitted `brain-qualification:*` scripts and are checked against plan-mode constants, flags, hashes, ports, paths, risks, and authorization gates. The verifier depends on the committed readiness hash, not `.orchestra` state.

14. **No-DB verification.** The consolidated entry point sanitizes database/provider/execution variables, runs the installed TypeScript compiler through native Node without cache-directory or fabricated OS-identity workarounds, and admits static/schema parity, controlled unit/integration tests, synthetic fixture verification, and original regression groups. It refuses database, provider, browser, authentication, or live-workflow execution.

15. **Evidence classification.** Static review, builds/typechecks, controlled unit tests, synthetic production-path tests, and in-process integration regressions are preparation evidence only. They explicitly do not establish real PostgreSQL persistence, migration recovery, authenticated ownership, Concierge workflow success, external delivery, or production behavior.

16. **Repository/publication state.** Preparation changes remain uncheckpointed and unpublished. `.orchestra`, authority DOCX files, the frozen FI-DAVE-001 scope, controllers, and unrelated changes are excluded. The outer controller must independently review the file-backed and executable evidence before applying the exact configured F.I. Forgot checkpoint/publication policy.

17. **Authorization boundary.** No new owner decision is needed for preparation review. A new explicit owner authorization is required before PostgreSQL installation/start/create/apply/revert/seed, credential handling, authenticated browser qualification, provider calls, deployment, cleanup mutation, or any external mutation. Real persistence and owner-workflow evidence remain deliberately unresolved.

Latest recovery: the identity-spoofing preloader was removed. The full no-database gate passed in the managed sandbox using native Node and installed TypeScript; an executable probe confirms OS identity APIs retain their original results or errors. The README and executable-plan.json are compared in full against actual plan-mode CLI output, including commands, arguments, environment, authorization requirements, and limitations. Independent acceptance remains pending after the prior rejection; no checkpoint or publication has occurred.
