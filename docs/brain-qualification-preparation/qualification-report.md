# Brain qualification execution report

The eleventh complete workflow passed on 12 September 2026. Actual PostgreSQL, API and offline browser evidence passed independent review after the exact Windows Sandbox session was stopped. This completes the admitted local qualification experiment; it does not declare the entire Brain completion program finished. Governed verification, checkpointing, publication and the broader authority-gap disposition remain pending.

This report supplies the twenty execution outcomes in the `brain-qualification-fb3484a` objective. Its portable contracts are the [qualification README](README.md), [executable plan](executable-plan.json), [fixtures](synthetic-fixtures.json) and [product build contract](product-build-contract.md). Machine-specific raw evidence remains ignored under `.orchestra`, rather than being committed as portable product source. Receipt paths below are repository-relative evidence locators for the originating workspace; their hashes identify the examined bytes.

## Accepted execution identity

The accepted attempt nonce is `87ee48bc-c33d-4cf4-9bb3-3aa5f645d4be`; its exact Sandbox session is `cf77c6ba-c834-4389-9cc0-7911e5bfa610`. Execution completed with exit code zero at `2026-09-12T19:36:16.9715874Z`. Exact session stop was recorded at `19:36:50.8824188Z` and independently checked against the session inventory.

| Evidence | SHA256 |
|---|---|
| `.orchestra/qualification/brain-qualification-durable-evidence.json` | `fb306eedcf648b7acc0eb76a37054264925a24824cc6291d1faf7cafc2bb4a2a` |
| `.orchestra/qualification/brain-qualification-postgres-restart.json` | `d75cbc3c21f8f2f400657be4e9a3fded840eaa7262e44c13cc144aee3bfcb462` |
| `.orchestra/sandbox-evaluation/output/browser/browser-receipt.json` | `df87993c7005bdc4941f22d9e32372a05427c6cf6570f29a1f5ab85adc9e7b93` |
| `.orchestra/sandbox-evaluation/output/browser/browser-artifact-manifest.json` | `858a8e9019b1969a7346a994f549bf679458b42b7d823ca6e523a483120ab4d8` |
| `.orchestra/sandbox-evaluation/live-final-independent-acceptance.json` | `fe60d2cc33e8a64b3e3373123ba444f58616f897d6f344f7ee91e31fbed29479` |
| `.orchestra/sandbox-evaluation/eleventh-semantic-restart-independent-assessment.json` | `0ab07b5d795ac66de9b52fe654a22023c825a714ee4917eb01c62a40355158a5` |

## Twenty requested outcomes

1. **PostgreSQL 16 provisioning.** The pinned EDB PostgreSQL 16.15 Windows runtime and trusted Microsoft runtime dependencies were obtained and independently checked before guest database execution. PostgreSQL ran inside the accepted Sandbox; there was no host PostgreSQL installation, service, PATH modification, pgAdmin or StackBuilder. The original archive acquisition was bounded host acquisition under program authority, not arbitrary guest networking.

2. **Exact target identity.** The executor observed PostgreSQL 16.15, UTF8, loopback `127.0.0.1:55432`, database `fi_forgot_brain_qualification`, application role `fi_forgot_brain_qualifier` and separate administrator `fi_forgot_brain_cluster_admin`. PGDATA was exactly `C:/Users/James.Massaro/Projects/fi-forgot/.orchestra/qualification/fi-forgot-brain-qualification-pgdata`. Identity, catalog and ownership guards preceded admitted operations. Credentials were guest-private and were not evidence or host authorization flags.

3. **Baseline and migrations.** The fresh disposable target used the accepted `bootstrap/0000-current.sql` baseline covering 41 tables. The five incremental migration files were not replayed. This proves fresh-target bootstrap; migration upgrades and historical production-data recovery remain unqualified.

4. **Fixture loading.** Real SQL loaded two synthetic owners and seven declared recipients. The accepted fixture SHA256 is `1591c1c7e2adeab1d5f253a3d0de00ee1a1c2cea07ad1acff316eb9c9e843e62`. Its explicit synthetic evaluation date, date offsets and 14-day preview preference support dated examples. Unknown source-observation timestamps stay null. Empty personal-recipient collections/text satisfy actual frontend DTO defaults without adding relationship facts.

5. **Persistence.** Actual PostgreSQL-backed snapshots cover both owners, lifecycle history, Opportunity timing/evidence/provenance/confidence/restraint and presentation, append-only feedback and separate action/outcome history. Owner A has five Opportunities and six retained temporal-history rows; owner B has two Opportunities and three rows. The seven exact owner/recipient question selections and actual empty follow-up table state are included in the durable snapshot. Fresh reads after restart and after browser execution compared the complete recorded snapshot.

6. **Restart persistence.** A real PostgreSQL stop/start retained system identifier `7684734655404322196`. Postmaster start advanced from `2026-09-12T19:35:32.631039Z` to `19:35:50.708920Z`; persistence assertions passed at `19:35:53.870Z`. A freshly restarted API read the same durable evidence. This qualifies the committed state described above, not just page reload or transport reconnection.

7. **Brain lifecycle.** Actual API/SQL assertions exercise relationship-memory edit/archive/restore, versioned observations, uncertain interpretation, supersession, hypothesis retirement/revival, active/inactive states, derived Opportunities, retained history, feedback and follow-through. Separately, the actual production evaluator/repository exercised premature, approaching relevance, valid now, stale and expired states in a real PostgreSQL transaction using explicit synthetic evaluatedAt values. Every transition was reloaded, then the transaction rolled back and committed history was compared unchanged. Those synthetic transition rows were not committed across restart.

8. **Ownership isolation.** Real owner-scoped reads and cross-owner rejections cover the synthetic recipients and their state, including next-question and fresh-updates. Caller-supplied development identities qualify repository ownership filtering only. Authentication, token security and production tenant-security assurances are not established.

9. **Concierge/API integration.** Real server projections preserved the original three presentation slots. Occurrence-scoped do-not-remind feedback reduced three slots to two without admitting the fourth candidate; withdrawal restored the original three and baseline projection. Both feedback events remained in history. User-reported completion and outcome were separate lineages; withdrawing the action retained the independent outcome and history. No user-reported completion or outcome is represented as independently verified fact.

10. **Browser.** The actual staged frontend reached `/concierge` for both synthetic owners in offline sandboxed Chrome. The independent review inspected screenshots, accessibility artifacts and 38 immutable response envelopes, with zero denied requests and zero page errors. Initial and returned Workspace assertions matched actual captured projections; Conversation/Workspace tab changes and focus were checked. The run showed three presented Opportunities for owner A and one for owner B. This does not claim every count from zero through three was separately rendered; the API suppression test independently covers no-backfill behavior. Question-panel evidence proves panel presence only, not wording quality, earned restraint or all question branches. Actual process observations established the exact executable, private profile, debugging pipe and absence of `--no-sandbox`; teardown observed zero Chrome processes and a released frontend port.

11. **Containment and preservation.** Post-reboot inspection found Containers-DisposableClientVM enabled and Windows Sandbox available. Initial inspection preserved the checkpoint and shared-controller baseline. The active Codex policy remained workspace-write/restricted-network, and its Windows sandbox setting remained elevated in the preservation observations. The Git-only Host Bridge hash remained `3496723374884170482f08cade6445e1a138cf7ffa11213c1ab26f46c3a88378`. The whole Codex configuration hash changed from `26f3f531fb1efb51b9e0a58f2168e7cfdc8d6b87840ca1554080376d13fe2718` to `e666b311a519662d6c72a325864266a9ba864479b45fc950e208f5eec3e27e09`, with observed last-write time `2026-09-12T19:12:36.0559113Z`. The writer and cause are unknown; no whole-file identity claim is made. These later observations are not represented as unavailable pre-reboot hashes. Independent containment proof preceded database work, and the eleventh run passed 29 executable preflight checks before separate execution acceptance. Networking and redirections were disabled, input was read-only, and negative probes/canaries tested the boundary. Loopback itself was available: this is not a claim of OS per-port firewall isolation. Later explicitly authorized product/controller corrections changed reviewed source; the entire repository is not claimed unchanged throughout the program. Frozen authority was not edited.

12. **Defects observed.** Ten earlier full attempts failed: password-file portability; address identity rendering; numeric loopback DNS and IPC shutdown behavior; inherited PostgreSQL pipe handles; missing synthetic preview profile; incorrect browser users-table identifier; JSONB property-order-sensitive duplicate history; unsupported browser navigation and missing personal GET admission; incomplete synthetic search DTO defaults; and denied actual question/health requests. These failures remain failures in their original attempt archives. Supporting diagnostics also exposed process-exit observation and subprocess recovery issues.

13. **Corrections applied.** Corrections added guest-private password input/UTF8, exact address observation, resolver-free loopback handling, robust bounded process observation/recovery, explicit synthetic profiles and DTO defaults, correct existing SQL identity, structural temporal evidence equality and deterministic mockup import ordering. Browser navigation/capture now follows real UI behavior. Exact production personal reads and provider-free question handlers are narrowly admitted by method/path/query/owner gates. Next-question retains its genuine understanding-materialization and follow-up-expiry semantics; it is not labeled read-only because it is GET. The harness primes and compares all seven selections and actual follow-up state. No follow-ups were invented to claim expiry coverage, and historical rows were not rewritten to hide defects.

14. **Post-correction automation.** The final combined qualification suite, typechecks, provider-import closure and full product build passed before staging. Focused regressions reproduce the observed defects and test corrected behavior, including structural JSONB equality, actual hydration/search construction and native HTTP/SQL handler parity and gate rejection. The final read-only evidence validator also passed independently after actual execution and exact disposal. Fresh governed verification remains a publication prerequisite.

15. **Independent acceptance.** Separate reviewers accepted containment, exact prepared launch and observed preflight before release. The actual semantic/restart assessment and final browser/disposal review bind this eleventh attempt's nonce, session and hashes. The final reviewer independently ran `node scripts/src/brain-qualification/verify-sandbox-evidence.mjs` with exit zero. Author freeze records alone were not acceptance. This report itself awaits its independent document review and governed mission disposition.

16. **Checkpoints and publication.** Pending. No correction checkpoint or origin publication is claimed by this report. The local shared Orchestra launcher owns fresh verification, independent acceptance, allowed-path checkpointing and policy-controlled publication. Separately authorized controller maintenance does not create general controller-edit authority.

17. **Final Git synchronization.** Pending governed repository/branch/origin/lineage and publication checks. The starting checkpoint was `fb3484adebb9767d1788baf0714d779d51afac51`; this report does not claim a new committed SHA, fresh remote equality or a clean final worktree.

18. **Disposable database final state.** The final guest observation found PostgreSQL stopped (`pg_ctl status` exit 3), owned marker/PG_VERSION 16 present, and ports 55432, 8080 and 25460 released. The exact Sandbox was subsequently stopped and its absence independently verified. The stopped, owned PGDATA remains preserved for governed verification. It has not been claimed deleted, and this report does not authorize cleanup or restart.

19. **Remaining unqualified behavior.** Production data, external providers, authentication, migration upgrades, follow-up expiry branches, question wording/earned-restraint quality and comprehensive Brain-authority coverage remain outside this experiment. All seven actual selections were profile-gap selections; empty follow-up state cannot qualify expiry. Synthetic decay transition history was rolled back, while committed temporal history was restart-tested. The broader authority-gap audit and governed publication are still required for the owner completion program.

20. **Genuine owner decisions.** No repeat Brain/database authorization is required or requested. Ordinary correction, evidence review and governed verification continue under existing authority. No unresolved owner decision is asserted here; any later real authority conflict or material scope change must be reported explicitly.
