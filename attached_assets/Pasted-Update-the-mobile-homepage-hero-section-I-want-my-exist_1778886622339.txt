Update the mobile homepage hero section.

I want my existing hero image to remain the background, but I need the headline and CTA to overlay the image cleanly without blocking the important parts of the image.

Mobile layout requirements:

1. Keep the hero image full-width and full-screen style on mobile.
2. Use `object-fit: cover`.
3. Keep Dave/doghouse visible on the right side.
4. Add a subtle dark gradient overlay on the left side of the hero image so the text is readable but the image still shows through.
5. Move the hero text block slightly higher than it currently is so it does not cover Dave’s face, the dog bowl, or the product cards.
6. Position the text block around:
   - left: 22px
   - top: 95px from the top of the hero image
   - max-width: about 330px
7. Keep the headline:
   “YOU FOCUS ON LIFE.
   WE REMEMBER EVERYTHING.”
8. Make “WE REMEMBER EVERYTHING.” red.
9. Replace the CTA button text with:
   “FIX MY FORGETTING →”
10. Make the CTA button short, bold, red, and easy to tap on mobile.
11. Do not add a box behind the text. Use the gradient overlay and text-shadow instead.
12. Make sure the CTA does not cover Dave’s face or the main product cards.
13. Keep desktop layout unchanged unless necessary.

Suggested CSS direction:

.hero {
  position: relative;
  overflow: hidden;
}

.hero img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 62% top;
}

.hero::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(
    90deg,
    rgba(0, 0, 0, 0.62) 0%,
    rgba(0, 0, 0, 0.42) 35%,
    rgba(0, 0, 0, 0.08) 68%,
    rgba(0, 0, 0, 0) 100%
  );
  pointer-events: none;
}

.hero-copy {
  position: absolute;
  z-index: 2;
  left: 22px;
  top: 95px;
  max-width: 330px;
}

.hero-copy h1 {
  margin: 0;
  font-size: clamp(34px, 8.8vw, 48px);
  line-height: 0.95;
  letter-spacing: -0.02em;
  color: white;
  text-shadow: 0 3px 18px rgba(0, 0, 0, 0.65);
}

.hero-copy .red {
  color: #d83232;
}

.hero-cta {
  margin-top: 18px;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: #c82127;
  color: white;
  padding: 15px 20px;
  border-radius: 8px;
  font-weight: 800;
  letter-spacing: 0.06em;
  text-decoration: none;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.35);
}

Please apply this in the existing codebase using the current class names where possible. If the existing class names are different, adapt the styles to the existing hero section instead of creating duplicate unused classes.